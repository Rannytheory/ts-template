{
    "name": "@repo/app",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "npx tsc",
        "start": "node ./dist/index.js",
        "test": "node --test",
        "cleanup": "rm -rf ./dist"
    },
    "dependencies": {
        "@repo/logger": "workspace:*"
    }
}