{
  "name": "ts-template",
  "version": "0.0.0",
  "dependencies": {
    "dotenv": "^16.6.1"
  },
  "scripts": {
    "start": "DEBUG=* pnpm --filter app run start",
    "build": "pnpm -r build",
    "dev": "pnpm run build && DEBUG=* pnpm --filter app run start",
    "test": "pnpm run build && pnpm -r test",
    "cleanup": "pnpm -r cleanup"
  },
  "devDependencies": {
    "@types/node": "^20.19.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.13.1"
}
